<html>
    <head>
        <title>We are Sorry</title>
        <link rel="stylesheet" type="text/css" href="style.css" />
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
        <div id="createAccountNotification"></div>
        <div id="userNameDisplay"></div>
        <div>Welcome {{userName}}. This page is not yet built out. </div>
        <div id="homeButtons">
            <br />
            <form action="/editAccount" method="get" id="attemptLogin">
                <input id='styledButton' type="submit" value="Edit Account Info" />
            </form>
            <br />
            <form action="/searchPage" method="get" id="searchPage">
                <input id='styledButton' type="submit" value="Search For Procedure" />
            </form>
        </div>
      
     <script>

            var userId = this.document.cookie;
            var submitBtn = document.getElementById("editInfo");

            submitBtn.addEventListener("click", function (event, context) {
                var reqBody = {
                    'userId': userId
                };
                var req = new XMLHttpRequest();
                req.open('POST', '/editAccount', true);
                req.setRequestHeader('Content-Type', 'application/json');
                reqBody = JSON.stringify(reqBody);

                req.send(reqBody);
            });

     </script>
    </body>
    
</html>
